export { NomadClient, getNomadClient, createNomadClient, setAuthToken, getAuthToken } from '@/api/client'

export const DEFAULT_BASE_URL = '/v1'
export const DEFAULT_WAIT_TIME = '5s'
export const DEFAULT_THROTTLE_MS = 2000
